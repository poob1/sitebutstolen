---
import { Icon } from "astro-icon";

export interface Props {
	href: string;
    src: string;
}

const { href, src } = Astro.props;
---

<a href={href}>
    <Icon class="w-8 h-8 hover:(opacity-70)" pack="mdi" name={src} />
</a>