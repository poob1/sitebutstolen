---
import "unocss-hmr-fix";

export interface Props {
    width?: string;
    height?: string;
    center?: boolean;
}

const { width, height, center } = Astro.props;
---

<img class={center ? "self-center" : ""} width={width} height={height} src="/logo.png" />